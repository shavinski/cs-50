#include <stdio.h>
#include <cs50.h>

// need to compile doing this: gcc hello.c -o hello -lcs50 
// extra comment to test


int main(void)
{
    for (int i = 0; i < 3; i++)
    {
        printf("meow\n");
    }
}